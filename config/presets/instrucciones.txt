[[STYLE_PRESET_START]]
(AQUÍ VA EL PROMPT DEL PRESET)
[[STYLE_PRESET_END]]

cd C:\PROYECTOS\tales-nextgen-studio
git status
git add .
git commit -m "Hardening: owner admin controls + secure health + remove exposed secrets"
git push origin staging

# IMPORTANTE
# Nunca guardes claves reales en archivos del repo.
# R2 / Supabase / Render / Stripe deben vivir solo en variables de entorno.
